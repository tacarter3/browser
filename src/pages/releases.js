import React from 'react';

const Releases = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>New Releases</h1>
    </div>
  );
};

export default Releases;